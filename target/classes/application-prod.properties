# Production Database Configuration
spring.datasource.url=jdbc:postgresql://your-prod-db-host:5432/hie_manager
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# Production Kafka Configuration
kafka.bootstrap.servers=154.120.216.119:9093,154.120.216.120:9093
kafka.security.protocol=SASL_PLAINTEXT
kafka.sasl.mechanism=SCRAM-SHA-256
kafka.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username="admin" password="075F80FED7C6";

# Production Kafka Producer Configuration
kafka.producer.security.protocol=${kafka.security.protocol}
kafka.producer.sasl.mechanism=${kafka.sasl.mechanism}
kafka.producer.sasl.jaas.config=${kafka.sasl.jaas.config}
kafka.producer.acks=all
kafka.producer.enable.idempotence=true
kafka.producer.max.block.ms=60000
kafka.producer.request.timeout.ms=60000
kafka.producer.retries=3
kafka.producer.retry.backoff.ms=1000

# Production Kafka Consumer Configuration
kafka.consumer.security.protocol=${kafka.security.protocol}
kafka.consumer.sasl.mechanism=${kafka.sasl.mechanism}
kafka.consumer.sasl.jaas.config=${kafka.sasl.jaas.config}

# Lab Routing Configuration
lab.routing.config.api.address=http://154.120.216.119:3007

# Production Logging Configuration
logging.level.zm.gov.moh.hie=INFO